"""Evaluation harness for the minicode agent.

Runs the agent against a set of self-contained coding tasks under different
ablation conditions, then reports resolve rate and token cost per condition so
each harness subsystem's contribution can be measured. See README.md.
"""
